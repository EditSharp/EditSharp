# EditSharp

A C# timeline video editing library: clips are node graphs, sources are pluggable, and rendering runs through ffmpeg.

Start with the [History namespace](api/EditSharp.History.yml) (the reviewed pilot) or browse the API from the menu.
