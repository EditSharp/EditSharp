namespace EditSharp.Components.Nodes
{
    /// <summary>Where a graph's result leaves it; every graph has exactly one, and it can't be removed.</summary>
    public abstract class OutputNode : Node { }
}
